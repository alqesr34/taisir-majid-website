# 🚀 شرح رفع المشروع على GitHub

## الخطوة الأولى: إعداد حساب GitHub

1. **انتقل إلى**: https://github.com
2. **اضغط** "Sign up" إذا لم تكن لديك حساب
3. **املأ البيانات** واتبع التعليمات
4. **تحقق من بريدك الإلكتروني**

---

## الخطوة الثانية: تثبيت Git

### على Windows:

1. **اذهب إلى**: https://git-scm.com/download/win
2. **اضغط على Download** (الإصدار الأخير)
3. **ثبت البرنامج**:
   - أكمل المراحل التالية
   - استخدم الإعدادات الافتراضية
4. **أعد تشغيل الـ Terminal** (أغلقها وافتحها من جديد)

### التحقق من التثبيت:
```powershell
git --version
```

---

## الخطوة الثالية: إنشاء مستودع على GitHub

1. **سجل دخول** إلى GitHub
2. **اضغط على** + (أعلى اليمين) → **New repository**
3. **امل البيانات**:
   - **Repository name**: `taisir-majid-website`
   - **Description**: `Official website for Taisir Majid Al-Qaissi`
   - **Public** (للجميع) أو **Private** (خاص)
   - ❌ لا تختر "Initialize with README" (لدينا واحد فعلاً)
4. **اضغط** "Create repository"

---

## الخطوة الرابعة: إعداد Git الإعدادات الأولية

في PowerShell/Terminal:

```powershell
# عيّن اسمك
git config --global user.name "Your Name"

# عيّن بريدك (استخدم نفس البريد المسجل في GitHub)
git config --global user.email "your-email@example.com"
```

---

## الخطوة الخامسة: رفع المشروع

في مجلد المشروع `i:\web`:

```powershell
# 1. ابدأ مستودع Git محلي
git init

# 2. أضف جميع الملفات
git add .

# 3. أنشئ التزام (Commit) أول
git commit -m "Initial commit: Add website project"

# 4. أضف المستودع البعيد (Remote)
git remote add origin https://github.com/YOUR_USERNAME/taisir-majid-website.git

# 5. اضغط (Push) الملفات إلى GitHub
git branch -M main
git push -u origin main
```

### ملاحظة:
عند `git push` قد يطلب منك اختيار طريقة المصادقة:
- استخدم **GitHub Token** (الأسهل)
  - اذهب إلى: https://github.com/settings/tokens
  - انسخ الـ Token والصقه في Terminal

---

## الخطوة السادسة: التحقق

1. **اذهب إلى** https://github.com/YOUR_USERNAME/taisir-majid-website
2. **تحقق** من وجود جميع الملفات
3. **شاهد أن المشروع موجود بنجاح** ✅

---

## أوامر مهمة أخرى

### إضافة تغييرات جديدة:
```powershell
# بعد كل تعديل
git add .
git commit -m "Description of changes"
git push
```

### عرض حالة المستودع:
```powershell
git status
```

### عرض سجل الالتزامات:
```powershell
git log
```

### نسخ مستودع موجود:
```powershell
git clone https://github.com/USERNAME/REPO_NAME.git
```

---

## النشر التلقائي من GitHub

### باستخدام Railway:

1. **اذهب إلى** https://railway.app
2. **اختر** "Deploy from GitHub"
3. **اختر المستودع** من قائمة مستودعاتك
4. **Railway** سيكتشف التطبيق تلقائياً
5. **سيُنشر تلقائياً** عند كل `git push`

### باستخدام Render:

نفس الخطوات كما في الأعلى، لكن على https://render.com

---

## نصائح مهمة

✅ **تأكد من إضافة** `.gitignore` (موجود بالفعل)  
✅ **لا ترفع** `messages.json` و `activities.json` التي تحتوي بيانات حقيقية  
✅ **غيّر** كلمة المرور قبل رفع المشروع  
✅ **اكتب رسائل التزام واضحة** (commit messages)  

---

## حل المشاكل

### "fatal: not a git repository"
```powershell
git init
git add .
git commit -m "Initial commit"
```

### "permission denied" عند Push
اطلب GitHub Token من: https://github.com/settings/tokens

### لا تظهر الملفات المحدثة
```powershell
git add .
git commit -m "Update files"
git push
```

---

## الخطوة التالية

بعد رفع المشروع على GitHub:

1. ☑️ ارفعه على Railway أو Render
2. ☑️ احصل على رابط دائم
3. ☑️ شارك الرابط مع الآخرين

وتم! موقعك الآن متاح للجميع على الإنترنت! 🎉

