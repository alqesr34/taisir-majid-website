# 🔧 استكشاف الأخطاء في Railway

## ✓ ما تحقق منه بالفعل

- ✅ التطبيق يعمل محلياً على http://localhost:8000
- ✅ جميع الملفات موجودة
- ✅ requirements.txt محدث
- ✅ Git مرتبط بنجاح

---

## 🔍 الخطوات للتحقق من الخطأ على Railway

### 1️⃣ **افتح Dashboard Railway**

1. اذهب إلى: https://railway.app/dashboard
2. اختر المشروع: `taisir-majid-website`
3. اضغط على "Logs" أو "Build Logs"

---

### 2️⃣ **ابحث عن الرسائل التالية:**

#### ❌ خطأ شائع: "ModuleNotFoundError"
```
ModuleNotFoundError: No module named 'flask'
```
**الحل**: أضف المتطلبة إلى `requirements.txt`

#### ❌ خطأ شائع: "Port"
```
Address already in use
```
**الحل**: استخدم `PORT` من البيئة

#### ❌ خطأ شائع: "Template not found"
```
TemplateNotFound: templates/index.html
```
**الحل**: تأكد من وجود مجلد `templates`

---

## 🛠️ الحل السريع

### الخطوة 1: تحديث requirements.txt

تأكد من وجود:
```
Flask>=2.2
gunicorn>=20.1.0
```

### الخطوة 2: تحديث web.py

في نهاية الملف، تأكد من:
```python
if __name__ == '__main__':
    import os
    port = int(os.environ.get('PORT', 8000))
    debug = os.environ.get('ENVIRONMENT') != 'production'
    app.run(host='0.0.0.0', port=port, debug=debug)
```

### الخطوة 3: Git Push

```powershell
cd i:\web
git add .
git commit -m "Fix production deployment"
git push origin main
```

Railway سينشر تلقائياً

---

## 📋 قائمة التحقق

- [ ] هل `requirements.txt` يحتوي على جميع المتطلبات؟
- [ ] هل `web.py` يستخدم `PORT` من البيئة؟
- [ ] هل مجلد `templates` موجود؟
- [ ] هل مجلدات `static` موجودة؟
- [ ] هل لا توجد أخطاء في `web.py`؟

---

## 💡 نصائح مهمة

1. **الملفات الثابتة** (`static/`):
   - يجب أن تكون في المكان الصحيح
   - تأكد من المسارات النسبية

2. **قاعدة البيانات** (JSON):
   - يتم حفظها محلياً
   - في Railway، قد تُفقد عند إعادة التشغيل
   - استخدم قاعدة بيانات خارجية للإنتاج (اختياري)

3. **المنفذ**:
   - استخدم دائماً: `int(os.environ.get('PORT', 8000))`
   - لا تثبّت المنفذ على 8000 مباشرة

4. **التصحيح** (Debug):
   - في الإنتاج: `debug=False`
   - في التطوير: `debug=True`

---

## 🚀 إعادة تشغيل على Railway

إذا كان كل شيء صحيحاً:

1. اذهب إلى: https://railway.app/dashboard
2. اختر المشروع
3. اضغط "Redeploy" أو "Trigger Deploy"
4. انتظر النشر الجديد

---

## ✅ اختبار بعد التحديث

بعد الدفع على GitHub:
1. Railway سينشر تلقائياً
2. انتظر حتى تظهر رسالة "Deployed"
3. افتح الرابط وتحقق

**الرابط:**
```
https://taisir-majid-website-production.up.railway.app
```

---

## 📞 معلومات إضافية

إذا كنت بحاجة:
- **دعم Railway**: https://railway.app/support
- **وثائق Flask**: https://flask.palletsprojects.com
- **وثائق Gunicorn**: https://gunicorn.org

