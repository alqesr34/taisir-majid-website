# دليل رفع الموقع على استضافة حقيقية (Render) 🚀

هذا الدليل يشرح كيفية رفع موقعك ليصبح متاحاً للجميع بشكل دائم ومجاني باستخدام خدمة Render.

## المتطلبات المسبقة (تم تجهيزها لك ✅)
- ملف `requirements.txt` (يحتوي المكتبات المطلوبة).
- ملف `Procfile` (يخبر الاستضافة كيفية تشغيل الموقع).
- ملف `wsgi.py` (نقطة الدخول الرئيسية للموقع).
- حساب على [GitHub](https://github.com) (مطلوب لرفع الكود).
- حساب على [Render](https://render.com) (الاستضافة).

---

## الخطوة 1: رفع الكود إلى GitHub

1.  قم بإنشاء مستودع جديد (New Repository) على GitHub.
2.  افتح موجه الأوامر (CMD) في مجلد المشروع ونفذ الأوامر التالية:

```bash
git init
git add .
git commit -m "Initial Deploy"
git branch -M main
git remote add origin <رابط المستودع الخاص بك>
git push -u origin main
```

*(إذا واجهت صعوبة في هذه الخطوة، يمكنك استخدام GitHub Desktop لرفع الملفات بسهولة).*

---

## الخطوة 2: إنشاء خدمة الويب على Render

1.  سجّل الدخول إلى [Render Dashboard](https://dashboard.render.com).
2.  اضغط على زر **New +** واختر **Web Service**.
3.  اختر **Build and deploy from a Git repository**.
4.  اربط حساب GitHub الخاص بك، ثم اختر المستودع (Repository) الذي أنشأته للتو.
5.  اضغط **Connect**.

---

## الخطوة 3: إعدادات الإطلاق (Deploy Settings)

ستظهر لك صفحة إعدادات، تأكد من البيانات التالية:

- **Name**: اختر اسماً لمشروعك (مثلاً: `my-news-site`).
- **Region**: اختر الأقرب لك (مثلاً: `Frankfurt`).
- **Branch**: `main`.
- **Root Directory**: اتركها فارغة.
- **Runtime**: `Python 3`.
- **Build Command**: `pip install -r requirements.txt` (عادةً تكون مكتوبة تلقائياً).
- **Start Command**: `gunicorn wsgi:app` (مهم جداً أن تكون هكذا).

---

## الخطوة 4: المتغيرات البيئية (Environment Variables)

للحماية، يجب إضافة كلمات المرور كمتغيرات وليس في الكود.
في قسم **Environment Variables**، أضف التالي:

| Key | Value |
| :--- | :--- |
| `PYTHON_VERSION` | `3.9.0` (أو أي إصدار حديث) |
| `SECRET_KEY` | (اكتب نصاً عشوائياً طويلاً وصعباً) |
| `ADMIN_USER` | (اسم المستخدم للأدمن، مثلاً `admin`) |
| `ADMIN_PASS` | (كلمة المرور للأدمن، اختر كلمة قوية) |
| `ENVIRONMENT` | `production` |

---

## الخطوة 5: الإطلاق (Deploy)

1.  اختر الخطة المجانية **Free**.
2.  اضغط **Create Web Service**.

سيبدأ Render في بناء موقعك. انتظر بضع دقائق حتى تظهر علامة ✅ خضراء وكلمة **Live**.

رابط موقعك سيكون شيء مثل: `https://my-news-site.onrender.com`

---

## ملاحظة هامة حول البيانات ⚠️
على الخطة المجانية في Render:
- الملفات المحملة (صور/فيديو) **قد تُحذف** عند إعادة تشغيل الخادم لأن نظام الملفات غير دائم (Ephemeral Filesystem).
- لحل هذه المشكلة في المستقبل، يُفضل ربط الموقع بخدمة تخزين خارجية مثل **AWS S3** أو **Cloudinary** لحفظ الصور والفيديوهات بشكل دائم.
- قاعدة البيانات الحالية (ملفات JSON) ستعود لحالتها الأصلية عند كل تحديث (Deploy) جديد.

استمتع بموقعك! 🎉
